export * from './common-types';
export * from './harmony';
interface FetchOptions extends RequestInit {
    /**
     * the region used for Sigv4 signing, if applicable
     */
    region?: string;
    /**
     * the service name used for signing. Defaults to 'execute-api'
     */
    service?: string;
}
interface AmazonXHR {
    new (): XMLHttpRequest;
    (): XMLHttpRequest;
}
import { Harmony } from './harmony';
declare global {
    interface Window {
        harmony: Harmony;
        fetch: (resource: RequestInfo, options?: FetchOptions) => Promise<Response>;
        Amazon: {
            IDP: {
                xhr: AmazonXHR;
            };
        };
    }
}
